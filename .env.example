# ========================================
# HOW TO USE LOCALLY
# ========================================
# 1. Copy this file: cp .env.example .env
# 2. Set DATABASE_URL below (required for manual data persistence)
# 3. For local Postgres, ensure PGSSL=false
# 4. Ensure manual data features are enabled (see below)
# 5. Run: node server.js
# 6. Test: Open http://localhost:3000/api/healthz to verify connectivity
#
# Required for full manual data functionality:
#   FEATURE_MANUAL_DATA=true
#   FEATURE_MANUAL_LIABILITIES=true
#   FEATURE_MANUAL_ASSETS=true
#   FEATURE_STATIC_DB=false
# ========================================

# Core server config
PORT=3000
BACKEND_URL=https://teller10-15a.onrender.com

# PostgreSQL connection (REQUIRED for manual data persistence)
# Local example: postgres://user:password@localhost:5432/teller_db
# Render example: postgres://user:password@host.render.com:5432/dbname
DATABASE_URL=

# For local Postgres without SSL, set PGSSL=false
# For Render/cloud Postgres, set PGSSL=true or remove this line
PGSSL=false

# Manual data features
FEATURE_MANUAL_DATA=true
FEATURE_MANUAL_LIABILITIES=true
FEATURE_MANUAL_ASSETS=true

# Demo/static data mode (disable to use live/proxied backend)
FEATURE_STATIC_DB=false

# Optional write guards
MANUAL_DATA_READONLY=false
MANUAL_DATA_DRY_RUN=false

# Optional: if you customize rent-roll table name
# MANUAL_DATA_TABLE=manual_data

